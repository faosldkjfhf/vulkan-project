find_package(glfw3 3.4 REQUIRED)
find_package(VulkanMemoryAllocator REQUIRED)
find_package(glm REQUIRED)
find_package(slang REQUIRED)
find_package(Vulkan REQUIRED)

add_library(engine engine.cpp src/vma.cpp src/core/window.cpp src/core/device.cpp src/rendering/renderer.cpp src/core/pipeline.cpp)
target_include_directories(engine PUBLIC include . ${Vulkan_INCLUDES})
target_link_libraries(engine PRIVATE math glm::glm slang::slang ${Vulkan_LIBRARIES} GPUOpen::VulkanMemoryAllocator glfw)
target_precompile_headers(engine PRIVATE pch.h)
